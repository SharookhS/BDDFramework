pipeline {
    agent any
    stages {
        stage('Git Checkout') {            
            steps {
                echo "------GitHub-------"  
                sh 'printenv'
            }
        }
    }
}
